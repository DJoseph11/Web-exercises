// I was trying to make a small program to take an input ie. number
//strings for the program to output the typeof type of the input
var checkingTypeOf = function(item) {
    var text = prompt("Enter Option:");
    return function(text) {
        console.log(typeOf item)
    }
    return

}
gg

checkingTypeOf();